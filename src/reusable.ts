/**
 * This file should outline the way reusable components are defined.
 */

// import { El } from './factory'
// import type { SetupArguments } from './methods/setup'

// export function reusable(el: keyof typeof El, setupFn: SetupArguments) {
//   const inst = El[el]()
//   inst.setup(setupFn)
//   return inst
// }
